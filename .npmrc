# Auto-install peer dependencies
auto-install-peers=true

# Shamefully hoist for better compatibility with tools that don't support pnpm's strict mode
# This helps with ESLint plugins and other tools that expect a flat node_modules
shamefully-hoist=true

# Strict peer dependencies (disabled for now to avoid warnings)
strict-peer-dependencies=false
