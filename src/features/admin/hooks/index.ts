/**
 * Admin Hooks Barrel Export
 *
 * Centralized exports for all admin-related React hooks.
 * Provides clean import paths for consuming components.
 *
 * @example
 * ```tsx
 * import { useDashboardOverview } from '@/features/admin/hooks'
 * ```
 */

export {
  useDashboardOverview,
  type UseDashboardOverviewResult,
  type TimeRange,
} from './use-dashboard-overview'
