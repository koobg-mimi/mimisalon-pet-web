/**
 * Admin UI Components
 *
 * CVA-based UI components for the admin dashboard.
 * All components follow the CVA pattern with variants and sizes.
 */

export {
  StatCard,
  statCardVariants,
  statCardHeaderVariants,
  statCardTitleVariants,
  statCardValueVariants,
  statCardIconVariants,
  statCardSubtitleVariants,
  type StatCardProps,
} from './stat-card'
